array<ui8, 256> ascii;
ui16 i = 0;
for (;i <= 255; ++i) {
    ascii[i] = i;
} 
array<ui32, 5> arr;
ui32 idx = 0;
arr[++++idx--] = 1;
idx = 0;
arr[idx++++] = 2^2;
 
function ui32 fff() {
    return 0;//fff(); 
}
struct A {
    ui32 val;
    bool b; 
} a;
 
a @= a;
lock(a); 
ui32 val = fff() + a.val;// + 2;
error(e) {
} 
unlock(a);
unlock(3);

 
/*sizeof(fff) + fff() + 2; 
array<function<fff>,3 * 4> af;
ui8 i = sizeof(af); 
array<ui32,(2 * i + 4)> arr1;
++arr1[20] = 123;
 
array<ui64, 2> aa;
aa[1] = 123456;
ui64 v64 = 1234;
ui32 i32 = 9 + lock(aa[1]);
unlock(aa); */